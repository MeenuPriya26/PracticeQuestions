package PracticeQuestionarres.Graph;/*
 * package Graph;
 * 
 * 
 * 
 * class Solution { // Function to return a list containing the DFS traversal of
 * the graph. public static void dfs(int node,boolean []
 * visited,ArrayList<ArrayList<Integer>> adj,ArrayList <Integer> storedfs){
 * visited[node]=true; storedfs.add(node);
 * 
 * 
 * for(Integer it :adj.get(node)){ if(visited[it]==false){
 * dfs(it,visited,adj,storedfs);
 * 
 * }
 * 
 * } }
 * 
 * public ArrayList<Integer> dfsOfGraph(int V, ArrayList<ArrayList<Integer>>
 * adj) { // Code here ArrayList <Integer> storedfs=new ArrayList <>(); boolean
 * visited[]=new boolean[V]; for(int i=0;i<V;i++){ if(visited[i]==false){
 * dfs(i,visited,adj,storedfs); } } return storedfs;
 * 
 * } }
 */